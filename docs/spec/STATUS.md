# STATUS — co jest zrobione z AUDIT.md (stan na 2026-09-01)

Ten plik jest nowszy niż `AUDIT.md`. Sekcje 3 i 6 audytu są **wykonane w całości**;
czytaj to zamiast nich. Reszta audytu (stan faktyczny, korekty do plików 00–07) nadal aktualna.

## Zamknięte (commit → zakres)

| Krok | Commit | Efekt |
|---|---|---|
| R3 | `1010bf2` | `ProductIngredient.ingredientId?`, `rawText`, `isAfterMayContain`, `matchConfidence`, `@@unique([productId, position])`; `IngredientAlias` |
| 07 | `b55513e` | `apps/api/src/modules/inci/` — parser (czysty, fixture'y), matcher L1–3, `UnmatchedToken`, `ProductSubmission`, kolejka `/admin/inci-queue`, import składu z etykiety w adminie |
| R1 | `354bfaa` | `pg_trgm` + `unaccent` + `tsvector` (kolumna generowana, `f_unaccent`, `f_join_text`), `SearchProvider`/`PostgresSearchProvider` (EAN → FTS → trigram), `GET /products/suggest`, `SearchLog`, matcher L4 (propozycje w kolejce) |
| 01 | `1fb7e2e` | `DataSource`, `ImportRun`, `isManuallyEdited`; `npm run import:obf` (idempotentny, `--dry-run`, `--resume`); atrybucja ODbL na stronie produktu, `/about-data`, `/admin/imports` |
| R2 | `a929d9c` | `ProductTraits` (+ `fingerprint vector(64)`, HNSW), `ProductTraitsService` = jedyne miejsce przeliczania `ingredientScore`; `CoarseMatchService` (przebieg A w SQL) + test właściwościowy `coarse ≥ exact`; `best-match`/`recommended` dwuprzebiegowe; alternatywy po fingerprincie; fasety boolean z licznikami, filtr `spf` |
| R6 | `bcfbc23` | `defaultLocale: 'pl'`, `/pl/**` → 301, AI domyślnie po polsku |
| R4 | `4c963e2` | Proxy `/api/**` w Nitro (`server/routes/api/[...path].ts`), `SameSite=Lax`, brak `Domain`, wykrywanie replaya refresh tokena (odwołuje wszystkie sesje) |
| R5 | `6736182` | `UserConsent` (wersjonowane zdarzenia), `@RequiresConsent` + `ConsentGuard` (403 `CONSENT_REQUIRED`), rejestracja z datą urodzenia (16+) i zgodami, `DELETE /account` z 7-dniową karencją + worker/`npm run account:purge`, `GET /account/export`, `SanitizedAIProvider` (klasa bazowa, test z e-mailem w każdym polu), walidator języka medycznego, `/privacy` |
| 9 | `735a715` | GitHub Actions (`checks` + `database` na `pgvector/pgvector:pg16`), `/health` + `/health/ready`, `AuditLog` + `/admin/audit` |
| 9 | `00149ff` | `MatchWeightSet` (`/admin/match-weights`), wagi wstrzykiwane do obu przebiegów; procent < 50 ukryty (pasma) |
| 9 | `6fc6bf1` | `ProfileExcludedIngredient` z `reason: ALLERGY \| PREFERENCE` — alergia = twardy filtr, preferencja = kara |
| 03 §3 | `a16f31d`→`2fc5f11` | **`ProductVariant`** w 3 migracjach (dodaj → backfill → drop): EAN/pojemność/zdjęcie/oferty/historia cen na wariancie, `products` bez kolumn opakowania; DTO ma `variants[]` + pola wariantu domyślnego; przełącznik pojemności na karcie produktu; admin edytuje wariant domyślny, oferta wskazuje wariant |
| 01 et. 2 | `e87b896`→`eaa1db0` | **Katalog OBF**: 18 tagów kategorii (twarz, SPF, oczy, maski, peelingi, demakijaż, dłonie, ciało, włosy) → **1263 produkty, 1076 widocznych (85%)**, 92% wierszy składu dopasowane. Parser: całość tokenu z ukośnikiem przed fragmentami, separatory •/(and)/&/kropka, prefiksy etykiet w 9 językach, sekcje OTC „Active/Inactive ingredients” z dawkami, segmentacja skanów o słownik (tylko pełne pokrycie słów); `ingredient-aliases.json` + `aliases:apply`; `import:obf --refresh`. Opisy EN+PL: seed (`ingredients.pl.json`) + partie `ingredient-prose/obf-batch-{1,2,3}.json` → **90% wierszy składu po polsku**, 419 produktów w całości. Duplikaty seed↔CosIng scalone aliasami (`4fced4a`); `HIDE_DEMO_DATA=true` chowa katalog demo, UI działa bez cen (`5709acf`) |
| 01 et. 1 | `94d418c` | **CosIng**: `npm run import:cosing` (glosariusz 33 654 wpisów przez EU Search API + Aneksy II–VI z CSV; idempotentny, `--dry-run`, `--resume`, `--max-pages`); po pierwszym przebiegu słownik ma **32 860** składników (65 alergenów zapachowych, 281 z Aneksu III, 236 z Aneksu II, 83 funkcje CosIng); `Ingredient.{cosIngRef,casNumber,ecNumber,innName,chemicalDescription,isFragranceAllergen,isRestricted,isProhibited,cosIngAnnex,restrictionNote,isManuallyEdited}`; tabele `IngredientFunction` + `IngredientFunctionOnIngredient`; po imporcie automatyczny rematch kolejki (`UnmatchedTokenService.rematchPending`) i odkrycie ukrytych produktów (`republishHidden`); `npm run ingredients:describe` — opisy AI (`AIProvider.describeIngredient`, structured output) tylko dla składników występujących w produktach; status regulacyjny na stronie składnika i badge na liście produktu; atrybucja CC BY 4.0 na `/about-data` |

## Sesja 2026-09-01 (niecommitowane — do commitu po akceptacji właściciela)

- **Opinie o portalu** (`AppReview`, migracja `20260831100000`): `/reviews` z formularzem (1 opinia na konto, bez edycji, delete+nowa), lista z sortowaniem i sumarycznym rozkładem gwiazdek, marquee `LandingReviews` na dole strony głównej, link w stopce, moderacja w `/admin/reviews` (ukryj/pokaż/usuń), gwiazdki w `BaseIcon` + komponenty `Review*`.
- **8 nowych funkcji** (fazy 1–8, wszystkie z bramkami i smoke testami):
  1. Cena za 100 ml na kartach + sort `price-per-100` (denormalizacja `Product.pricePerHundred`, migracja `20260901100000`, utrzymywana w admin/seed/`prices:demo`).
  2. PAO: `Category.paoMonths` (backfill wg kroku rutyny) + `Product.paoMonths` (override w adminie), półka pokazuje „otwarty/zużyj do/po terminie" + akcje otwarcia/zużycia (migracja `20260901110000`).
  3. Dupe finder: `GET /products/:id/dupes` (fingerprint pgvector + overlap), publiczna strona `/dupes`, przycisk na stronie produktu, link w stopce.
  4. Diff składów w porównywarce (`CompareIngredientsDiff` — wspólne/tylko-w + % zbieżności dla pary; czysty frontend).
  5. Alerty regulacyjne: `RegulatoryChange` (migracja `20260901120000`), diff Aneksów II/III w `applyAnnexes` (bezpiecznik >5% słownika), `GET/POST /shelf/regulatory-alerts(/seen)`, baner na dashboardzie, badge na półce, badge „Aneks II" w składzie.
  6. Zmiana formuły: `ProductFormulaRevision` (migracja `20260901130000`), hash składu przy `applyLabel` tylko z importów źródłowych, `ProductDto.recentFormulaChange` (<90 dni) + rozwijany diff na stronie produktu, lista w `/admin/formula-changes`.
  7. Planer AM/PM: `GET /shelf/routine-plan` — czysty `buildWeekPlan` (SPF rano; retinoid wieczorami naprzemiennie; złuszczanie limitowane wrażliwością, nigdy z retinoidem; maska raz w tygodniu), siatka tygodnia w zakładce rutyny.
  8. Dziennik skóry: `SkinDiaryEntry` na `BeautyProfile` (kasacja ze zgodą; migracja `20260901140000`), moduł `/diary` za `BEAUTY_PROFILE_HEALTH`, strona `/diary` (kalendarz+wpis+statystyki mies./mies.), w eksporcie RODO. **Wersja zgody podbita do `health-2026-09-01`** (hint wspomina dziennik) — istniejący użytkownicy (w tym demo) muszą ją ponowić w profilu/onboardingu.
- **Wyszukiwarka w nagłówku (2026-09-01, front + backend)**: inline `<form>` (desktop + menu mobilne) zastąpiony ikoną otwierającą pełnoekranową nakładkę `AppSearch`. Tło i panel to dwa osobne `<Transition>` — tło animuje `backdrop-filter` od `backdrop-blur-[0px]` do `backdrop-blur-md` (uwaga: `backdrop-blur-none` w Tailwind 4 ustawia zmienną na **pustą** wartość, przez co deklaracja jest nieprawidłowa i blur nie interpoluje). Escape, klik w tło (`@click.self`) i zmiana trasy zamykają, scroll strony zablokowany. Trzy strefy wizualne: białe pole wyszukiwania, wyniki na `bg-canvas/75`, biała stopka; kafelki `ProductSearchTile` to białe karty (miniatura, marka, nazwa, badge dopasowania, cena) z hoverem jak `ProductCard`.
  - `GET /products/preview?q=` (`SuggestQueryDto`, min. 2 znaki, `@OptionalAuth`) → `ProductSummaryDto[]`: `rankedCandidates` z `PostgresSearchProvider` (60 kandydatów → 6 wyników), filtr widoczności i alergenów, `decorate()` dokłada Personal Match. **Nie pisze do `SearchLog`** — liczy się tylko zatwierdzone wyszukiwanie, żeby nie zaśmiecać statystyk każdym znakiem.
  - `GET /products/popular-queries` → `string[]`: `GROUP BY lower(btrim(query))` na `search_logs` z ostatnich 30 dni, tylko `resultCount > 0`; czyste helpery w `search/popular-queries.ts` (normalizacja, dedup, limit długości 2–40) z testami. Kolumny w `search_logs` są **camelCase i wymagają cudzysłowów** w surowym SQL (`"createdAt"`, `"resultCount"`) — model nie ma `@map` na polach. Gdy log jest pusty, front spada na 5 kuratorowanych haseł z i18n.
  - Obie trasy zadeklarowane **przed** `@Get(':slug')`, inaczej przechwytuje je trasa produktu. Filtr alergenów wyciągnięty z `buildWhere` do `allergenExclusion()` i współdzielony z podglądem.
  - Front: `refDebounced` 250 ms, `watch: false` + ręczne `execute()`, szkielety w czasie ładowania; rekomendacje pustego stanu nadal z `GET /discover` (`key: 'search-feed'`, leniwie przy pierwszym otwarciu).
- **Ulubione na karcie produktu — naprawa (2026-09-01)**: serce działało wyłącznie na `/products`, bo `ProductCard` emitował `favorite`, a listener wpinała tylko ta jedna strona — na `/discover`, `/dashboard`, stronie głównej, w `ProductAlternatives` i na stronie składnika przycisk był martwy. Dodatkowo nakładka rozciągniętego linku (`after:absolute after:inset-0`) przechwytywała klik, bo serce nie miało `z-10` (przycisk porównania miał). `ProductCard` sięga teraz po `useShelf()` sam, tak jak już robił z `useCompareStore()` — usunięte prop `isFavorite` i emit `favorite`, a wraz z nimi prop `favoriteIds` i przekazywanie emitu przez `ProductGrid`. Stan półki ładuje `layouts/default.vue` (`onMounted` + `watch` na `isAuthenticated`), więc `favoriteIds` jest wypełnione na każdej stronie. Testy: stub `useShelf` w `tests/setup.ts`, test klikania sprawdza `aria-pressed` zamiast emitu; osobny test pilnuje `z-10`, bo jsdom nie liczy layoutu i sam klik przechodziłby mimo błędu.
- **Toasty przy ulubionych — naprawa (2026-09-01)**: `toggleFavorite` potwierdzało tylko ścieżkę POST (produkt spoza półki). Gałąź PATCH — produkt **już leżący na półce** — nie pokazywała nic, więc przy koncie z zapełnioną półką większość kliknięć w serce była niema. Sama kolejka toastów była sprawna (potwierdzone testem `BaseToaster`). Doszły klucze `SHELF.FAVORITED` / `SHELF.UNFAVORITED` (pisane bezosobowo — istniejące `{name} dodany` zakładają rodzaj męski i psują się przy nazwach żeńskich typu „Maska…"), `useToast` dostał `clear()` i limit `MAX_VISIBLE_TOASTS = 3` (najstarszy wypada), a `useShelf` pierwsze testy: harness w `tests/setup.ts` (stuby `useState`/`useApi`/`useApiMessage`, przełączniki `setSignedIn`/`setApiHandler`). Uwaga: `useToast.ts` woła `ref()` w zasięgu modułu, więc w setupie testów musi być importowany **dynamicznie**, po przypisaniu globali.
- **Kolejka INCI — rozpoznanie i naprawa przeniesień wyrazów (2026-09-02)**: wpis o „~184 tokenach-śmieciach" był nieaktualny i mylący co do natury problemu. Faktycznie **2201 tokenów PENDING**, dotykających **659 z 1208 aktywnych produktów** — ponad połowa katalogu pokazywała niepełny skład, bo nierozpoznane wiersze są odfiltrowane z publicznego DTO. To w większości nie marketing, tylko uszkodzone nazwy: literówki OCR (`helianthus annus`, `qua`, `alpha-isomethyl lonone`), synonimy (`purified water`, `vitamin e`) i nazwy przeniesione przez koniec wiersza.
  - Hipoteza „myślnik = separator listy, parser powinien dzielić po `' - '`" została **sprawdzona i odrzucona**: po podziale tylko 30% części trafiało w słownik, bo `sodium hyature - nate`, `citro - nellol`, `cetyl palmi - tate` to **przeniesienia wyrazów** ze skanów. Poprawna operacja jest odwrotna — sklejanie. Podział byłby aktywnie szkodliwy.
  - `joinHyphenatedToken` w `inci-parser.ts` (regex `/\s*-\s+|\s+-\s*/g` — łapie tylko myślnik ze spacją po którejś stronie, więc `peg-100`, `c12-15`, `1,2-hexanediol` zostają nietknięte) + poziom `HYPHEN_REPAIR` (confidence **0,9**) i przebieg `matchHyphenatedRemainder` w `InciMatcherService`. Sklejona forma jest przyjmowana **wyłącznie** przy dokładnym trafieniu w słownik, więc prawdziwa lista `aqua - glycerin` nigdy nie zlepi się w wymyślony składnik.
  - Repair świadomie siedzi w **matcherze, nie w parserze**: `normalized` jest już zapisane w bazie, więc zmiana parsera wymagałaby reimportu, a tak wystarcza `rematchPending()`.
  - Nowy skrypt `npm run inci:rematch -w @kosvia/api` (`scripts/rematch-tokens.ts`) — sam rematch, bez efektów ubocznych `aliases:apply`/`import:cosing`, które były dotąd jedynymi wywołaniami `rematchPending`.
  - **Efekt na bazie dev**: 95 tokenów rozwiązanych, 115 wierszy etykiet, 53 produkty. PENDING 2201 → 2106, nierozpoznane wiersze 2337 → 2222, produkty z lukami 659 → 639. Wśród odzyskanych wierszy **8 alergenów zapachowych i 10 składników z ograniczeniami** — w tym `Arachis Hy - pogaea (Peanut) Oil` (olej arachidowy), który wcześniej był niewidoczny dla twardego filtra alergii.
  - **Krok 2 — kształt separatorów (2026-09-02)**: 88 ze 115 dopasowań miało **różną liczbę** separatorów po obu stronach (`cocoglucoside` vs `Coco-Glucoside`), więc generowanie wariantów tokenu złapałoby tylko 27 — potrzebny był klucz „luźny" liczony obustronnie. `looseSeparatorKey` (usuwa `[- /]`) + poziom `SEPARATOR_SHAPE` (0,9) i przebieg `matchSeparatorShape`; klucz, do którego pasuje więcej niż jeden wpis słownika, jest **odrzucany**, nie zgadywany. Efekt: **113 tokenów, 154 wiersze, 123 produkty** (`Methyl Paraben`→`Methylparaben`, `IsopropylPalmitate`→`Isopropyl Palmitate`, `Coca midopropyl Betaine`→`Cocamidopropyl Betaine`).
  - **Krok 3 — aliasy (2026-09-02)**: 10 wpisów w `ingredient-aliases.json` po sprawdzeniu, że cel istnieje w słowniku: `Purified Water`→Aqua, `Vitamin E`→Tocopherol, `Ethanol`→Alcohol, `Tocopherol Acetate`→Tocopheryl Acetate (TYPO), `Alpha-Isomethyl Lonone`→Alpha-Isomethyl Ionone (TYPO), `1,3-Butylene Glycol`→Butylene Glycol, `Yellow 5`→CI 19140, `Red 33`→CI 17200, `Green 5`→CI 61570, `Polypodium Leucotomos Extract`→…Leaf Extract. Dało 10 tokenów, 47 wierszy, 41 produktów. `kojic acid dipalmitate`, `arabinan`, `thermus thermophilus ferment` **naprawdę nie istnieją** w słowniku — zostawione.
  - **Krok 4 — akcje zbiorcze w `/admin/inci-queue`**: `POST /admin/inci/queue/bulk/accept-suggestion` i `.../bulk/ignore` (`BulkTokenDto`, max 200 id), zadeklarowane **przed** trasami `:id` — zweryfikowane w logu startu Nesta. `BulkResolutionDto` w `@kosvia/shared` świadomie **nie zwraca liczby produktów**: `mapToIngredient` raportuje ją per token, a ten sam produkt bywa dotykany przez kilka tokenów, więc suma podwajałaby. UI: kolumna zaznaczenia + pasek akcji (zaznacz wszystkie na stronie / przyjmij sugestie / ignoruj / wyczyść), zaznaczenie kasowane przy zmianie strony i zakładki.
  - **Krok 5 — przegląd sugestii zamiast klikania (2026-09-02)**: właściciel nie robi ręcznej pracy na danych, więc 44 sugestie ≥ 0,9 przejrzane przeze mnie: **40 przyjętych, 4 odrzucone**. Odrzucone i dlaczego: `water water` → hydrolat z trawy cytrynowej (trigram daje 1.00, bo „water" powtarza się w długiej nazwie); `hydroxystearic linoleic oleic polyglycerides` → w słowniku są **dwa** wpisy różniące się jedną literą (`Linolenic/Oleic` i `Linolenic/Linoleic`); `rosmarinus officinalis stem leaf` → token nie rozstrzyga między `Oil` a `Water`, oba istnieją; `hydrolyzed pea protein vegetable protein` → dwie sklejone nazwy. Jedna sugestia **poprawiona**: `hypericum peforatum flower leaf stem extract` wskazywała 6-roślinną mieszankę, a w słowniku jest dokładny `Hypericum Perforatum Flower/Leaf/Stem Extract`.
  - **Krok 6 — automat na literówkach**: pasmo 0,75–0,9 (397 sugestii) rozwiązane regułą zamiast oglądania po jednej — przyjmujemy token, gdy jest w **odległości Levenshteina 1** od dokładnej nazwy słownikowej i **żaden inny wpis** nie jest tak blisko. Odległość 2 **odrzucona po sprawdzeniu próbki**: przeskakuje znaczące przedrostki (`polyglyceryl-3`→`polyglycerin-3` to ester vs poliol, `hydroxypropyltrimonium`→`di`hydroxypropyltrimonium to inna cząsteczka). Przy odległości 1 groźne okazały się **cyfry** (`peg ppg-10 1`→`peg ppg-10 2` to inny materiał), więc doszedł warunek identycznego ciągu cyfr po obu stronach — odsiał 18 fałszywek. Wynik: **270 tokenów, 307 wierszy, 192 produkty**.
  - Wszystkie decyzje zapisane jako **aliasy w `ingredient-aliases.json`** (407 aliasów w 261 celach), nie jednorazowym skryptem — przechodzą przez `aliases:apply`, są w gicie i przeżywają `db:reset`. Do analizy zakładałem `fuzzystrmatch`; **usunięte po użyciu**, żeby nie robić dryfu schematu.
  - **Bilans całości**: PENDING **2201 → 1673**, nierozpoznane wiersze **2337 → 1676**, produkty z lukami **659 → 500** (z 1208 aktywnych, czyli 55% → 41%). Odzyskano m.in. 8 alergenów zapachowych i 10 składników z ograniczeniami.
  - **Krok 7 — homoglify (2026-09-02)**: `foldToAscii` składało tylko znaki diakrytyczne, więc cyrylickie i greckie sobowtóry były wycinane jako nie-łacińskie: `Аqua` z U+0410 zostawało jako `qua`. Doszła mapa 27 homoglifów. Naprawa siedzi w **parserze**, nie w matcherze, bo informacja o oryginalnym znaku jest w `rawSamples`, a nie w zapisanym `normalized` — istniejące 18 wierszy naprawia się dopiero przy ponownym imporcie. Pułapka lintu: negowana klasa `[^\u0000-\u007f]` łamie `no-control-regex`, poprawnie jest `[\u0080-\uFFFF]`.
  - **Segmentacja długich ciągów — świadomie NIE robiona osobnym kodem**: sonda na żywym słowniku pokazała, że **80 tokenów (122 wystąpienia)** rozbiłoby się czysto, ale zastosowanie tego wymaga rozbicia jednego `ProductIngredient` na kilka z przesuwaniem `position` (jest `@@unique([productId, position])`). Tę logikę ma już poprawnie ścieżka importu, więc właściwym narzędziem jest `import:obf --refresh`, a nie równoległy kod.
  - **`--refresh` jako właściwe narzędzie**: omija warunek „bez zmian" w `obf-import.service.ts`, więc przepuszcza każdy produkt ponownie przez parser, segmenter i wzbogacony słownik. Uruchomiony przebieg po wszystkich 44 tagach kategorii.
  - **Nic nie wymaga ręcznego zatwierdzania przez właściciela.** Kolejka INCI to wewnętrzna lista robocza, nie bramka: produkty z lukami działają, pokazują tylko mniej składników. UI z akcjami zbiorczymi jest opcją do triażu, nie wymogiem.
  - **Zostaje**: ogon ~1670 tokenów — głównie proza obcojęzyczna (`suya dayan kl`, `maharashtra`, ostrzeżenia po koreańsku) i ciągi bez separatorów. Ignorowanie ich to wyłącznie higiena kolejki: `ignore` zmienia sam status tokenu, w danych produktów nie zmienia nic, a token IGNORED **nie wraca** przy ponownym imporcie (`record()`) — więc nie jest darmowe.

- Ceny demo: patrz sekcja Środowisko. Baza po restarcie Dockera działa; serwery dev uruchomione w tle.
- Testy: API 220, web 40, i18n:check czysty, format czysty. Nowe klucze i18n z formami mnogimi (`|`).

## Sesja 2026-09-02 — pakiety Free/Premium (niecommitowane — do commitu po akceptacji właściciela)

Kompletny system planów wg `kosvia-free-premium-spec.md` (backend + front).

- **Modele** (migracja `20260902100000`, pisana ręcznie + `migrate deploy`, bo `migrate dev` chce resetu przez znany fałszywy drift): `Subscription` (1:1 z userem; `period MONTHLY|YEARLY`, `state ACTIVE|CANCELED|EXPIRED`, `provider` domyślnie `MANUAL`, `providerReference`, `expiresAt` — gotowe pod App Store/Google Play/Stripe bez przebudowy uprawnień), `SubscriptionPlan` (cena w groszach per okres, edytowalna w adminie; brak wiersza = domyślne 19,99/149,99 z `DEFAULT_PLAN_PRICING`), `UsageCounter` (`@@unique(userId, metric, periodStart)`), `PersonalMatchAnalysis` (dedup pełnej analizy per produkt/miesiąc).
- **Semantyka statusu**: `User.subscriptionStatus` zostaje znacznikiem efektywnym — `PREMIUM` = premium teraz (także CANCELED z niewykorzystanym okresem), wygaśnięcie sprawdzane leniwie (`expireIfDue` w guardzie/overview) przesuwa na `CANCELLED`. `JwtStrategy` dokłada `subscriptionStatus` do `AuthenticatedUser` (ten sam mechanizm co natychmiastowa degradacja roli).
- **Centralizacja**: `modules/subscription/` (`@Global()` jak Prisma): `EntitlementService` (jedyne miejsce reguł Free/Premium; `PLAN_LIMITS` w `@kosvia/shared` to jedno źródło limitów dla API i frontu), `UsageService` (limit = warunkowy `updateMany(used < limit)` — odporny na równoległe requesty; pełna analiza = transakcja insert-dedup + inkrement), `SubscriptionService` (cennik + cykl życia), `PremiumGuard` + `@RequiresPremium()` (403 `PREMIUM_REQUIRED` wzorem ConsentGuard), limity rzucają 403 `PLAN_LIMIT_REACHED` z `metric` i `limit` w body.
- **Endpointy**: `GET /subscription/plans` (@Public — cennik dla strony pakietów), `GET /subscription` (plan + subskrypcja + zużycie + `usageResetsAt`), `GET/PATCH /admin/subscription-plans(/:period)` (audytowane interceptorem jak reszta admina). `PATCH /admin/users/:id` z `subscriptionStatus` przechodzi przez `SubscriptionService.applyAdminStatus` (+opcjonalny `subscriptionPeriod`) — nadanie Premium tworzy ręczną subskrypcję bez daty końca.
- **Bramki** (wszystko po stronie backendu): AI chat — kredyt zużywany **przed** providerem (brak kosztu API po limicie), refund przy błędzie providera; pełna analiza = strona produktu (`GET /products/:slug`) — po limicie strona działa z wynikiem ogólnym + `matchLimitReached`, Premium dostaje `personalMatch.breakdown` (surowe `contributions()`); porównywarka >2 produktów, `GET /shelf/routine-plan`, `POST /routine/build`, `GET/POST /shelf/regulatory-alerts*`, `GET /ai/products/:slug/why` — premium; alerty cenowe 1/20 (także reaktywacja PATCH), półka 10/∞, dupes i alternatywy przycinane do 3 (`lockedDupeCount`), dziennik 7 dni (`historyLimited`, bez statystyk m/m), analiza półki Free = tylko `gap`+`balance` (duplikaty/overlap premium), PAO na półce premium. Dashboard filtruje to samo.
- **Front**: `/pricing` (publiczna) — switcher Miesięcznie/Rocznie z badge’em `-X%` liczonym z żywych cen (`yearlySavingsPercent`, zero hardcodu), karty wg spec §8, „Porównaj wszystkie funkcje” → `PricingComparisonModal` (tabela §10), karta zużycia dla zalogowanych; CTA Premium otwiera uczciwy modal (płatności w przygotowaniu — bez fikcyjnej płatności, bez „Wkrótce” jako CTA). `LandingPricing` bierze cenę z API i linkuje do `/pricing`. `useSubscription()` (useState, nie Pinia — stan serwerowy), `isPremium` w auth store, `PremiumPrompt` (wspólny upsell), paywalle w naturalnych punktach: AI (licznik + blokada kompozytora), półka (baner pełnej półki + toast przy 11.), alerty, panel dopasowania (limit + breakdown + teaser „Dlaczego?”), planer w zakładce rutyny, dziennik, dupes, alternatywy. Tray porównań liczy `maxItems` z planu. Admin: `/admin/subscriptions` (ceny/waluta/aktywność + podgląd oszczędności + nota o przyszłym providerze), users.vue ma select planu.
- **Seed**: wiersze `SubscriptionPlan` + subskrypcja MONTHLY dla `demo@kosvia.app`.
- **Testy**: API 268 (nowe: UsageService — atomika i dedup, EntitlementService — lazy expiry, 2. alert, 11. produkt, overview, PremiumGuard — kody 403, AI — 6. wiadomość nie woła providera/nie zapisuje, refund; diary — trim 7 dni), web 51 (przeliczenia cen + aktualizacja stubów). Lint/typecheck/format/i18n:check czyste w obu appkach; smoke SSR `/pricing` i landing OK.
- **Świadome decyzje**: „pełna analiza” = wejście na stronę produktu z personalizowanym wynikiem (listy/karty nie zużywają limitu, ten sam produkt w miesiącu liczony raz); eksport RODO nie obejmuje jeszcze `Subscription`/`UsageCounter` — dodać przy najbliższej zmianie eksportu; brak workera wygaszania subskrypcji (lazy wystarcza przy braku płatności).

## Decyzje podjęte po drodze (odstępstwa od 00–07)

- **05 §2** — bez denormalizacji `brand_name`/`category_name`: marka i kategoria w rankingu przez trigram + JOIN. Denormalizacja wróci naturalnie z `ProductTraits`, gdy profil zapytań tego zażąda.
- **05 §7** — fingerprint: wymiary 0–19 = 20 tagów słownika, 20–63 = hash id składników `isActiveIngredient` (44 koszyki) zamiast ręcznej listy aktywów.
- **04 §2** — przebieg A dostaje listę id z Prismy (`select id` po filtrach), a nie filtry w SQL. Przy ~100 tys. produktów bez filtrów trzeba przenieść filtry do SQL przebiegu A.
- **02 §4** — konto w karencji **nie jest** zablokowane: logowanie działa, profil pokazuje baner z anulowaniem (brak mailera na link „zmieniłam zdanie").
- **02 §5** — eksport synchroniczny (JSON w odpowiedzi), nie link 24 h.
- **07** — rematch po zmianie aliasu filtruje w pamięci wiersze `ingredientId IS NULL`; przy milionach wierszy dodać `normalizedText` na `ProductIngredient`.
- **01 et. 1** — funkcje CosIng są tabelą (`IngredientFunction`), ale `Ingredient.tags` zostaje tablicą stringów: tagi to wejście scoringu i mapują się z funkcji CosIng deterministycznie (`FUNCTION_TAGS` w `cosing-mapper.ts`); szerokie funkcje (SKIN CONDITIONING) nie dają tagu. Import nie dotyka `tags`, `sensitivityImpact`, `isActiveIngredient` na wpisach ręcznych (`isManuallyEdited`) ani na wpisach, które już mają tagi.
- **01 et. 1** — nazwy CosIng, które po `normalizeToken` zlewają się w jeden token (nawiasy są usuwane — ok. 3%, egzotyczne polimery/peptydy), są pomijane jako `duplicate-name`; `normalizedName` musi być jednoznaczne dla matchera.
- **01 et. 1** — opisy składników mają wersję EN (`description`, `functions`, `commonName`, `concerns`) i PL (kolumny `…Pl`); API wybiera po `Accept-Language` (`@RequestLocale()`, front wysyła aktywne locale w `01.api.ts`), fallback EN. Describer generuje obie naraz wyłącznie z faktów CosIng i tłumaczy 139 wpisów z seeda; provider offline zwraca `null`.
- **01 et. 1** — EU Search API ucina paginację na 10 000 trafień (`max_result_window`), więc glosariusz jest czytany w zakresach `substanceId` po pierwszej cyfrze (`planIngredientRanges`); zakres od `"0"` API odpowiada całym zbiorem, dlatego pierwsza cyfra zaczyna się od 1.
- **01 et. 1** — klucz do EU Search API pochodzi z publicznego configu aplikacji CosIng (`assets/env-json-config.json`); jeśli KE go zmieni, trzeba podmienić `SEARCH_API_KEY` w `cosing-client.ts`.
- Nierozpoznane składniki nie są widoczne w publicznym `ProductDto` (filtr `hasMatchedIngredient`) — decyzja UX odłożona.

## Otwarte (osobne zadania, w tej kolejności)

1. **Testy lokalne i poprawki UI (faza bieżąca, 2026-08-31).** Właściciel klika po aplikacji (`npm run dev`, opcjonalnie `HIDE_DEMO_DATA=true`) i zgłasza poprawki wyglądu/drobiazgi — obsługiwać na bieżąco. Kolejka INCI: patrz osobny wpis poniżej — wcześniejsze „~184 tokeny-śmieci (marketing w obcych językach)" było błędne; 29 ukrytych produktów OBF ma zepsute etykiety źródłowe.
2. Akcja „scal jako wariant" w adminie — dwa produkty tej samej formuły w różnych pojemnościach są dziś dwoma produktami, dopóki nie przyjdą z tym samym EAN-em. (`PriceAlert` i półka zostają na produkcie — decyzja.)
3. **Deployment — odłożony decyzją właściciela (2026-08-31)**: najpierw testy lokalne i poprawki; brak domeny (kosvia.pl niekupiona) i decyzji o hostingu. Gdy wróci: zacząć od planu do akceptacji (VPS vs platforma zarządzana, backupy, migracje przy starcie); w repo jest CI, nie ma nic do wystawiania aplikacji.
4. Kolejne rundy katalogu: `import:obf` jest idempotentny — ponowne przebiegi dociągają nowości; po każdym większym imporcie partia opisów (`SELECT` z liczbą wystąpień → plik prose → `ingredients:describe --from=…`).
5. **Proweniencja słownika CosIng (2026-09-01) — przed publicznym uruchomieniem.** (a) `Ingredient` nie ma znacznika importu: `ImportRun` loguje przebieg, ale z samego rekordu nie odczytasz, kiedy i z jakiego snapshotu pochodzi — dodać `sourceImportedAt` (opcjonalnie `sourceRunId`) ustawiane przy upsercie w `CosIngImportService` i pokazać datę snapshotu na `/about-data`; przy Aneksach (zmiany typu 2023/1545) atrybucja bez daty jest myląca. (b) `cosIngRef` jest, ale stopka „Source: CosIng" na stronie składnika nie linkuje do konkretnego wpisu — zrobić deep link. (c) Zweryfikować warunki użycia samego endpointu, nie tylko ogólną politykę reuse KE: dane mogą być CC BY 4.0, a dostęp do EU Search API mieć własne warunki — tym bardziej że `SEARCH_API_KEY` pochodzi z publicznego configu aplikacji CosIng (patrz Decyzje). Wariant docelowy: oficjalny eksport glosariusza (aneksy już bierzemy z CSV) — znosi problem klucza i daje naturalny `source_version` = data pliku.
6. Feedy afiliacyjne (`01` etap 4) — po publicznym uruchomieniu (rejestracja wydawcy wymaga działającej strony).
7. Keyset (`05` §5) — gdy będzie nieskończony scroll. Autocomplete w nagłówku (`05` §6) zrobiony nakładką `AppSearch` (patrz sesja 2026-09-01); `GET /products/suggest` nie ma już konsumenta we froncie — zdecydować, czy zostaje jako publiczne API, czy znika.
8. Retencja 24 mies. i maile (karencja usunięcia) — wymagają mailera; `User.lastActiveAt` już jest.
9. Poza kodem: prawnik (`/privacy` to draft inżynierski, DPA z dostawcą LLM, DPIA, warunki endpointu CosIng — p. 5); `User-Agent` klienta OBF ma placeholder `kontakt@kosvia.pl` — podmienić po zakupie domeny.

**Po `db:reset` (kolejność odtworzenia danych):** `db:seed` → `import:cosing` → `aliases:apply` → `ingredients:describe --from=` dla `ingredients.pl.json` i `ingredient-prose/obf-batch-{1,2,3}.json` → `import:obf` per kategoria.

## Środowisko

- Baza dev: kontener `kosvia-postgres` (`pgvector/pgvector:pg16`, port **5433**); `.env` i `apps/api/.env` wskazują 5433. Homebrew Postgres na 5432 nie jest już używany.
- 16 migracji; `prisma migrate diff` pokazuje fałszywy drift (GIN, kolumna generowana, `vector`) — źródłem prawdy jest `migrate status` + `migrate deploy`.
- `@kosvia/shared` jest czytane przez API z `dist` — po zmianie wartości runtime: `npm run build -w @kosvia/shared`.
- Prettier tylko z roota (`.prettierignore` obejmuje `phrases.generated.ts`, `docs/spec`, migracje); po edycji locale: `npm run sync:phrases -w @kosvia/api`.
- Skrypty: `import:obf`, `import:cosing`, `ingredients:describe`, `aliases:apply`, `traits:recompute`, `account:purge`, `prices:demo` (`-w @kosvia/api`).
- **Ceny demo (2026-09-01)**: katalog OBF nie ma prawdziwych ofert, więc `prices:demo` generuje deterministyczne ceny (widełki per krok rutyny, 3 fejkowe drogerie `drogeria-demo-*`, `DataSource` `demo-prices`, 6 tyg. historii) i przelicza `lowestPrice`. `--dry-run` raportuje, `--remove` cofa wszystko do zera — **wykonać przed podpięciem feedów afiliacyjnych**. Po `db:reset` odpalić ponownie po `import:obf`.
- Konta demo: `demo@kosvia.app / Password123!` (wszystkie zgody), `admin@kosvia.app / Admin123!`.
